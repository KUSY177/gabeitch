# Документация разработчика проекта "gabeitch"

Данная документация описывает структуру проекта, модули, классы и игровые системы 2D‑платформера *gabeitch*.  
Проект разработан на Python с использованием библиотеки **Arcade** и имеет модульную архитектуру, обеспечивающую удобство сопровождения и расширения.

---

# 1. Структура проекта

gabeitch/

  main.py                 — точка входа в игру
  
  game.py                 — основной игровой цикл
  
  player.py               — класс игрока
  
  enemy.py                — класс врагов
  
  level_loader.py         — загрузка уровней из JSON
  
  constants.py            — глобальные константы проекта

  assets/
  
    sprites/              — спрайты персонажей и объектов
    
    tilesets/             — тайлсеты уровней
    
    sounds/               — звуковые эффекты
    

  levels/
  
    level1.json
    
    level2.json
    
    ...


Код

Каждый модуль выполняет строго определённую функцию и взаимодействует с другими компонентами через чётко определённые интерфейсы.

---

# 2. Описание модулей и классов

## 2.1 main.py

Главная точка входа в приложение.

**Назначение:**
- инициализация окна игры;
- запуск основного игрового класса `GameView`;
- настройка параметров экрана.

---

## 2.2 game.py

Содержит основной игровой класс.

### Класс `GameView(arcade.View)`

**Отвечает за:**
- игровой цикл (update, on_draw);
- обработку пользовательского ввода;
- управление состояниями игры (пауза, смерть, переход между уровнями);
- загрузку уровней через `LevelLoader`;
- обновление всех игровых объектов (игрок, враги, платформы).

**Основные методы:**
- `on_draw()` — рендеринг сцены;
- `on_update(delta_time)` — обновление логики;
- `on_key_press()` — обработка нажатий;
- `load_level(name)` — загрузка уровня.

---

## 2.3 player.py

### Класс `Player(arcade.Sprite)`

**Отвечает за:**
- движение персонажа;
- прыжки;
- анимации;
- обработку столкновений;
- взаимодействие с объектами уровня.

**Основные поля:**
- скорость движения;
- скорость прыжка;
- состояние (idle, run, jump, fall);
- текущая анимация.

**Основные методы:**
- `update()` — обновление состояния;
- `update_animation()` — переключение кадров;
- `jump()` — выполнение прыжка.

---

## 2.4 enemy.py

### Класс `Enemy(arcade.Sprite)`

**Отвечает за:**
- движение врагов по заданной траектории;
- обнаружение столкновений с игроком;
- нанесение урона;
- проигрывание анимаций.

**Основные методы:**
- `update()` — логика патрулирования;
- `reverse_direction()` — смена направления движения.

---

## 2.5 level_loader.py

### Класс `LevelLoader`

Модуль загрузки уровней, созданных в редакторе **Tiled**.

**Отвечает за:**
- чтение JSON‑файлов уровней;
- создание платформ, врагов, объектов;
- размещение игрока;
- загрузку тайлмапов и слоёв.

**Основные методы:**
- `load_level(path)` — загрузка уровня;
- `create_platforms()` — создание платформ;
- `create_enemies()` — создание врагов.

---

## 2.6 constants.py

Модуль глобальных констант.

**Содержит:**
- размеры окна;
- скорость игрока;
- параметры физики;
- пути к ассетам;
- настройки уровней.

Пример:

```python
SCREEN_WIDTH = 1280
SCREEN_HEIGHT = 720
PLAYER_SPEED = 5
GRAVITY = 1.0
```

# 3. Игровые системы

## 3.1 Система физики

### Назначение
Система физики отвечает за корректное поведение объектов в игровом мире.

### Функции:
- обработка гравитации;
- определение столкновений со стенами и платформами;
- корректировка позиции при коллизиях;
- определение состояния «на земле»;
- поддержка прыжков и падений.

### Реализация:
- используется `arcade.PhysicsEnginePlatformer`;
- физика обновляется в методе `on_update()`.

---

## 3.2 Система анимаций

### Назначение
Обеспечивает визуальную динамику персонажей и объектов.

### Функции:
- переключение кадров анимации;
- выбор анимации в зависимости от состояния (idle/run/jump/fall);
- синхронизация движения и визуализации;
- поддержка анимаций врагов.

### Реализация:
- методы `update_animation()` в классах Player и Enemy;
- использование спрайт-листов.

---

## 3.3 Система уровней

### Назначение
Загружает и формирует игровой мир на основе данных из JSON.

### Функции:
- чтение уровня, созданного в Tiled;
- создание платформ;
- размещение врагов;
- размещение объектов;
- определение стартовой позиции игрока.

### Реализация:
- класс `LevelLoader`;
- уровни хранятся в каталоге `levels/`.

---

## 3.4 Система ввода

### Назначение
Обрабатывает действия игрока.

### Функции:
- движение влево/вправо;
- прыжок;
- пауза;
- взаимодействие с объектами;
- смена состояний игрока.

### Реализация:
- методы `on_key_press()` и `on_key_release()` в `GameView`.

---

## 3.5 Система столкновений

### Назначение
Обеспечивает корректное взаимодействие объектов.

### Функции:
- проверку контактов игрока с платформами;
- определение удара по врагу;
- получение урона;
- активацию объектов;
- корректировку позиции при пересечении коллайдеров.

### Реализация:
- встроенные методы Arcade для проверки пересечений спрайтов.

---

# 4. Описание каталогов проекта

## 4.1 Каталог `assets/`

Содержит все ресурсы игры.

### assets/sprites/
- спрайты персонажей;
- спрайты врагов;
- объекты и предметы;
- анимации.

### assets/tilesets/
- тайлсеты для уровней;
- декоративные элементы.

### assets/sounds/
- звуки прыжков;
- звуки ударов;
- звуки взаимодействий.

---

## 4.2 Каталог `levels/`

Содержит уровни в формате JSON.

Каждый уровень включает:
- тайловые слои;
- платформы;
- врагов;
- объекты;
- стартовую позицию игрока.

Примеры:
- `level1.json` — обучающий уровень;
- `level2.json` — уровень средней сложности.

---

# 5. Архитектура проекта

Проект построен по принципу модульной архитектуры.

## 5.1 Основные подсистемы

### Подсистема рендеринга

#### Отвечает за:
- отрисовку спрайтов;
- отображение уровней;
- визуальные эффекты.

#### Использует:
- `arcade.Sprite`;
- `arcade.SpriteList`;
- `arcade.View`.

---

### Подсистема физики

#### Включает:
- гравитацию;
- обработку столкновений;
- корректировку положения объектов.

#### Использует:
- `arcade.PhysicsEnginePlatformer`.

---

### Подсистема управления

#### Обрабатывает:
- нажатия клавиш;
- прыжки;
- движение;
- взаимодействие с объектами.

#### Реализована в методах:
- `on_key_press()`;
- `on_key_release()`;
- `on_update()`.

---

### Подсистема загрузки уровней

#### Отвечает за:
- чтение JSON‑файлов;
- создание платформ;
- размещение врагов;
- загрузку тайлмапов.

#### Реализована в классе:
- `LevelLoader`.

---

### Подсистема анимаций

#### Включает:
- переключение кадров;
- выбор анимации;
- синхронизацию движения и визуализации.

#### Реализована в методах:
- `Player.update_animation()`;
- `Enemy.update_animation()`.

---

# 6. Взаимодействие компонентов

main.py  
→ запускает GameView  
→ использует LevelLoader  
→ создаёт Player, Enemy, Platforms  
→ обновляет в on_update()  
→ отображает в on_draw()

---

# 7. Обработка ошибок и исключений

## Ошибки загрузки уровня
Если JSON‑файл отсутствует или повреждён:
- выводится предупреждение;
- игра не завершается аварийно;
- загружается fallback‑сцена или пустой уровень.

## Ошибки ассетов
Если отсутствует спрайт или звук:
- используется заглушка;
- выводится предупреждение.

## Ошибки ввода
- некорректные клавиши игнорируются.

---

# 8. Возможности расширения

Архитектура проекта позволяет легко добавлять:
- новые уровни;
- новых врагов;
- новые объекты;
- улучшенную физику;
- систему урона и здоровья;
- меню, HUD, интерфейс;
- сохранение прогресса;
- систему достижений.

---

# 9. Заключение

Документация разработчика описывает структуру проекта, назначение модулей, классов и игровых систем.  
Она служит основой для сопровождения, расширения и дальнейшей разработки 2D‑платформера **gabeitch**.
